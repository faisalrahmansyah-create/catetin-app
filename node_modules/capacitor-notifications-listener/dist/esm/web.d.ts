import { WebPlugin } from '@capacitor/core';
import type { AndroidNotification, NotificationsListenerPlugin } from './definitions';
export declare class NotificationsListenerWeb extends WebPlugin implements NotificationsListenerPlugin {
    readonly errorMessage = "Method will never be implemented. Whose notifications you want to read?";
    attachListener(eventName: 'notificationRemovedEvent', callback: (info: AndroidNotification) => void): AndroidNotification;
    attachListener(eventName: 'notificationReceivedEvent', callback: (info: AndroidNotification) => void): AndroidNotification;
    startListening(): Promise<void>;
    restoreCachedNotifications(): Promise<void>;
    stopListening(): Promise<void>;
    requestPermission(): Promise<void>;
    isListening(): Promise<{
        value: boolean;
    }>;
    replacePackagesWhitelist(): Promise<void>;
}
