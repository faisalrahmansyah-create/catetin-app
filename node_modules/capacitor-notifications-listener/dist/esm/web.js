// browser can't read your notifications.
import { WebPlugin } from '@capacitor/core';
export class NotificationsListenerWeb extends WebPlugin {
    constructor() {
        super(...arguments);
        this.errorMessage = 'Method will never be implemented. Whose notifications you want to read?';
    }
    attachListener() {
        throw new Error(this.errorMessage);
    }
    startListening() {
        throw new Error(this.errorMessage);
    }
    restoreCachedNotifications() {
        throw new Error(this.errorMessage);
    }
    stopListening() {
        throw new Error(this.errorMessage);
    }
    requestPermission() {
        throw new Error(this.errorMessage);
    }
    isListening() {
        throw new Error(this.errorMessage);
    }
    replacePackagesWhitelist() {
        throw new Error(this.errorMessage);
    }
}
//# sourceMappingURL=web.js.map